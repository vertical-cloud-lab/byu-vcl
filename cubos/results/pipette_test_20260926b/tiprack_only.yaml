# OFFLINE CHECK ONLY, not a run config. Exercises the new tip-rack anchor on its
# own: bare-nozzle pickup at A1, drop, home. Used with the committed gantry and
# deck files to trace what pick_up_tip / drop_tip command. See README.md.
protocol:
  - home:
  - pick_up_tip:
      position: tip_rack.A1
  - drop_tip:
      position: tip_rack.A1
  - home:
