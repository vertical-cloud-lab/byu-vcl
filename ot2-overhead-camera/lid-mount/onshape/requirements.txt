requests       # onshape_api.py
playwright     # onshape_ui.py; then `playwright install chromium`, or pass --chrome to use an installed Chrome
cadquery>=2.4  # both import the shared parameters from ../cad/lid_mount.py
